struct Point {
	x: real,
	y: real,
	z: real,
}

struct Polygon {
	points: list<Point>,
}

struct List<T> {
	slice: RawSlice<T>,
	refs: u32,
}

impl<T> List<T> {

}

enum Foo {
	Bar {
		foo: &Foo,
	},
	Baz {
		
	},
}

enum Option<T> {
	Foo {},
	Bar {},
}

Option::Foo {
	...
};

let points = [];

points = append(points, []);

&points[2].x

guard Borrow<T> { ... }

guard MutexGuard<T> { ... }
